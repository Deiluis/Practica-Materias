#include "Registro.h";

// /*
//  * Funcion constructora
// */
// Registro* registroInicializar(Registro* reg)
// {
//     // Registro* p = registros;
    
//     // p->periodo = (char*)malloc(PERIODO_TAM);
    
//     // if(p->periodo == NULL) return NULL;

//     // p->nivel = (char*)malloc(NIVEL_TAM);

//     // if(p->nivel == NULL) return NULL;

//     // p->indiceICC = (char*)malloc(INDICE_TAM);

//     // if(p->indiceICC == NULL) return NULL;

//     //return registros;
// }

// /*Funcion destructora, libera la memoria asignada dinamicamente a Registro*/
// void registroDestruir(Registro* reg)
// {
//     // Registro* p = reg;
    
//     // free(p->periodo);
//     // free(p->nivel);
//     // free(p->indiceICC);
// }
